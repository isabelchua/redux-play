import React, { Fragment } from "react";
import CircularProgress from "@material-ui/core/CircularProgress";

export default () => (
	<Fragment>
		<CircularProgress color="inherit" size="8rem" thickness={2} />
	</Fragment>
);
